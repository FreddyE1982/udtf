name: Copy Dataset to Hugging Face

on:
  push:
    branches:
      - main

jobs:
  copy-dataset:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Git
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"

    - name: Clone original dataset repository
<<<<<<< HEAD
      run: git clone --mirror https://user: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@huggingface.co/datasets/blanchon/udio_dataset
=======
      run: git clone --mirror https://huggingface.co/datasets/blanchon/udio_dataset/
>>>>>>> c47b48ee6d085a757a0f00ccf7b74552886f7574

    - name: Push to new Hugging Face repository
      run: |
        cd original-dataset
        git remote add new-dataset https://huggingface.co/datasets/FreddyE1982/udtf
        git push new-dataset --mirror
